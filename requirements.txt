fastapi>=0.110.0
uvicorn>=0.28.0
websockets>=12.0
aiofiles>=23.2.1
aiohttp>=3.9.3
humanize>=4.9.0
psutil>=5.9.8
pydantic>=2.6.0
python-multipart>=0.0.9
yt-dlp
libtorrent
torrentp

# Speed test uses Cloudflare's edge over the Python stdlib — no library needed.
# For full drive S.M.A.R.T. metrics (temperature / power-on hours / health):
#   • Linux:   install smartmontools (`sudo apt install smartmontools`) and run with sudo
#   • Windows: install smartmontools (`winget install smartmontools`) for SATA/USB drive
#              temperature & power-on hours, then run DiskPulse as Administrator.
#              (Windows' built-in storage cmdlets only expose those metrics for NVMe drives.)